<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.niyien.gyroflow"
    android:installLocation="auto"
    android:versionCode="100"
    android:versionName="1.6.3">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- In-app update: allows requesting the system package installer for the
         downloaded APK (user still confirms in the installer UI). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <!-- Package visibility (Android 11+): MainActivity.resolveDocumentsUi() probes
         these packages so the file picker can be aimed at the AOSP/GMS DocumentsUI
         explicitly. Some ROMs (HyperOS) reroute an implicit ACTION_OPEN_DOCUMENT to
         their own picker, which declares screenOrientation="behind" and therefore
         inherits this app's forced landscape - a layout it does not support. Without
         these <queries> entries PackageManager.resolveActivity() returns null and we
         silently fall back to that implicit path. -->
    <queries>
        <package android:name="com.google.android.documentsui" />
        <package android:name="com.android.documentsui" />
    </queries>

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:extractNativeLibs="true"
        android:hardwareAccelerated="true"
        android:label="Gyroflow(NiYien)"
        android:icon="@mipmap/icon"
        android:allowNativeHeapPointerTagging="false"
        android:allowBackup="true"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">
        <activity
            android:name="com.niyien.gyroflow.MainActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:label="Gyroflow(NiYien)"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.OPENABLE" />

                <data android:host="*" />
                <data android:port="*" />
                <data android:mimeType="application/octet-stream" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\..*\\.gyroflow" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.gyroflow" />
                <data android:pathPattern=".*\\..*\\..*\\.gyroflow" />
                <data android:pathPattern=".*\\..*\\.gyroflow" />
                <data android:pathPattern=".*\\.gyroflow" />
                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.OPENABLE" />

                <data android:mimeType="video/mp4" />
                <data android:mimeType="video/quicktime" />
                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="gyroflow" />
            <meta-data android:name="android.app.arguments" android:value="" />
            <meta-data android:name="android.app.extract_android_style" android:value="minimal" />
            <meta-data android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" android:resource="@xml/device_filter" />
        </activity>

        <!--provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths"/>
        </provider-->

        <!-- In-app update: exposes the downloaded APK in the update cache to
             the system package installer via a content URI. Kept separate from
             Qt's (commented) qtprovider so the two never collide. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.niyien.gyroflow.updateprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_provider_paths"/>
        </provider>
    </application>
</manifest>
