Put merged lens package source files here.

Expected layout:

- `lens/` for lens-related payload files
- `camera_db/` for camera database payload files

They are packaged together by `_scripts/package_lens.py` into one versioned `cbor.gz` bundle.
