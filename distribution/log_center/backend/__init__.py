"""log_center backend package — pure Python modules consumed by
the pywebview entry point (`log_center.py`). Independent from
`distribution/control_center/backend/` per design D2 (configuration
and runtime context must not couple).
"""
